# Dependencies (reinstalled in Docker)
node_modules/

# Next.js build output (rebuilt in Docker)
.next/

# Config only needed at build time — not in runtime image
# (standalone output copies what it needs)
tailwind.config.ts
postcss.config.mjs
tsconfig.json

# Dev environment
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Logs
npm-debug.log*
